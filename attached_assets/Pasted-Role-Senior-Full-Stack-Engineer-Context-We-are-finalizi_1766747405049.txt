Role: Senior Full-Stack Engineer
Context: We are finalizing "ReviewGuard" (static HTML/JS) for deployment to Vercel.
Current Stack: Vanilla JS, Tailwind CSS, i18n via `autoTranslate.js`.

**OBJECTIVE:**
Upgrade the app with new "Phygital" features based on user intent analysis, and configure the project for Vercel deployment.

**TASK 1: IMPLEMENT NEW FEATURES**

1.  **The Instant Counter Stand (QR Generator)**
    * **Dependency:** Add `<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>` to the `<head>` of `index.html`.
    * **New File:** Create `public/locales/qrStand.js`.
    * **Logic:**
        * Create a class `QRGenerator`.
        * Function `renderCard()`: Injects a hidden HTML container with a 4x6" card design.
        * Design: Professional border, Header: "Love our Service?" (translatable), Large QR Code container, Footer: "Review us on Google - It takes 15 seconds."
        * Function `printCard()`: Unhides the container, triggers `window.print()`, and hides it again (or uses `@media print` CSS to only show the card).
    * **UI:** Add a button "🖨️ Print Counter Card" in the Dashboard area (next to the SMS preview).

2.  **The Link Finder (UX Fix)**
    * **Update:** In the Settings Modal (`index.html`), rename the "Google Review Link" input label to **"Your Google Review Link"**.
    * **Add:** A helper text/tooltip below it: *"Go to Google Maps > Your Business > Share > Copy Link."*

3.  **Landing Page Copy (SEO & Intent)**
    * **Update:** `index.html` Hero & Features to match high-intent keywords.
    * **Headline:** "Turn Happy Customers into 5-Star Reviews (And Stop the Bad Ones)."
    * **Feature 1:** "Damage Control: Intercept negative feedback via SMS before it hits Google."
    * **Feature 2:** "Phygital Growth: Print your official QR Counter Stand instantly."

**TASK 2: VERCEL CONFIGURATION**

1.  **New File:** Create `vercel.json` in the root directory.
2.  **Content:**
    ```json
    {
      "cleanUrls": true,
      "trailingSlash": false,
      "rewrites": [
        { "source": "/", "destination": "/index.html" },
        { "source": "/audit", "destination": "/audit-widget.html" }
      ]
    }
    ```

**TASK 3: TRANSLATIONS**
* Update `public/locales/en.json` and `es.json` to include the new strings for the QR Stand ("Love our Service?", "Scan to Review").

**OUTPUT:**
Write the code for `qrStand.js`, `vercel.json`, and the necessary HTML/JS updates.